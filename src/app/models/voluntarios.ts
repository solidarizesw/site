export interface Voluntarios {
  id: number;
  nome: string;
  email: string;
  curso: string;
  motivacao: string;
  status: string;

  // não usar para pegar o arquivo por enquanto. apenas testar as validações
}
